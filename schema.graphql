type Query {
	list: [Post!]!
}

input PostInput {
	userId: ID!
	content: String!
}

type Mutation {
	createPost(post: PostInput): Post!
}

type Post {
	id: ID!
	content: String!
	user: User!
}

type User {
	id: ID!
	name: String!
	posts: [Post!]
}
